# About / manifesto carousel

Place **5** portrait-friendly images (paths used in `TennisHomeMain`):

- `about-1.webp` … `about-5.webp`  
  (slides 1–4 portrait ~1201×1800; slide 5 landscape ~2000×1333)

Then set `USE_MEDIA_PLACEHOLDER` to `false` in `src/config/tennis-assets.ts`.
