# Hero carousel (`TennisHomeMain`)

Place **5** wide images here (recommended **2560×1440** `.webp`):

- `hero-1.webp` … `hero-5.webp`

After files exist, set `USE_MEDIA_PLACEHOLDER` to `false` in `src/config/tennis-assets.ts`.
