# Schedule / program cards

Place **5** images (used as schedule card visuals):

- `schedule-1.jpg` … `schedule-5.jpg` (suggested ~540×801)

Then set `USE_MEDIA_PLACEHOLDER` to `false` in `src/config/tennis-assets.ts`.
